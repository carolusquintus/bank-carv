server:
  port: 8081
  servlet:
    context-path: /account-service

spring:
  application:
    name: bank-account-service
  datasource:
    url: jdbc:sqlserver://localhost:1433;database=bank-account-db;encrypt=true;trustServerCertificate=true;
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: sa
    password: kNhiMcy61ut5!
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml